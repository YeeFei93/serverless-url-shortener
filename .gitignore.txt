# Ignore Terraform state files and lock files
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# Ignore Lambda deployment package (if zipped locally)
lambda/function.zip

# OS / Editor files
.DS_Store
Thumbs.db

# Log files
*.log
